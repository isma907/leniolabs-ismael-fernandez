import { AdvancedFilterPipe } from './advanced-filter.pipe';

describe('AdvancedFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new AdvancedFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
